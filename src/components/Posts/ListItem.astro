---
const {post} = Astro.props;
---
<li class="flex flex-col space-y-2 md:space-y-4">
    <section class="aspect-w-6 aspect-h-2 sm:aspect-h-3 rounded-lg bg-skin-primary-muted overflow-hidden">
        <a href={`/posts/${post.slug}`}>
            <img alt={post.heroImage.title} src={post.imageUrl} class="object-center object-cover">
        </a>
    </section>
    <div class="bg-skin-accent-muted text-black py-1.5 px-2.5 flex justify-between items-center rounded-md">
        <span class="text-sm font-medium uppercase">
            Recipe
        </span>
        <span class="text-xs">5min Read</span>
    </div>
    <article class="">
        <a href={`/posts/${post.slug}`}>
            <h3 class="text-base font-bold text-skin-secondary">{post.title}</h3>
        </a>
        <small class="mt-1 text-sm text-skin-secondary-muted">Lorem ipsum dolor sit amet consectetur adipisicing elit</small>
    </article>
</li>